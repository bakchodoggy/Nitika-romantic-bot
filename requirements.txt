fastapi
uvicorn
python-telegram-bot
openai
pydantic
h11
httpx
starlette
aiohttp
# Optionally, for logging flexibility (already in Python stdlib, but 3rd party logging if you use it elsewhere)
# loguru
# Add any other dependencies used in your custom modules below