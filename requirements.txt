# Core API dependencies
fastapi
uvicorn[standard]

# Telegram bot (pin version for stability!)
python-telegram-bot==20.8

# AI/OpenAI
openai==0.28.1

# Data handling and async HTTP
pydantic
httpx
aiohttp

# Starlette and h11 are already pulled in by FastAPI/uvicorn, but can be explicit:
starlette
h11

# If you use logging enhancements, uncomment below:
# loguru

# --- Add your own modules' dependencies here (if any) ---
# For example, if you need Pillow for images:
# pillow

# For file I/O or data storage (if you use them):
# aiosqlite
# sqlalchemy

# Make sure all imports in your bot and API code have corresponding entries here!